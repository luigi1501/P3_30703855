<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Insertar Categoría</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="../stylesheets/style.css">
</head>
<body>
    <form action="/insertima" method="post">
   
        <h2>Agregar Imagen</h2>
        <label for="url">url:</label>
        <input type="text" name="url" required >


    
    <label for=""></label>
    <select name="destacado" id="destacado">
        <option value="">--Seleccione--</option>
        <option value="Si">Si</option>
        <option value="No">NO</option>
    </select>

    <select name="producto_id" required>
        <option value="">--Seleccione--</option>
        <% if (producto) { %>
            <% for (let i = 0; i < producto.length; i++){  %>
                <% if (producto) { %>
                    <option value="<%= producto[i].id %>" id="producto_id" name="producto_id" required><%= producto[i].name %></option>
                <% } %>
            <% } %>
        <% } else { %>
                    <option value="">No existen productos en el sistema</option>
            <% }   %>
    </select>
            <input type="submit" value="Insertar" href="/tabimagen">
        </form>
</body>
</html>